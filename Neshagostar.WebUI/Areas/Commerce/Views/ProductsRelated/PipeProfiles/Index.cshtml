@model IEnumerable<Neshagostar.DAL.DataModel.CommerceRelated.ProductsRelated.PipeProfile>

@{
    ViewBag.Title = "Index";
}

<h2>لیست انواع پروفیل</h2>

<p>
    @Html.ActionLink("ثبت", "Create", new { area="Commerce", controller="PipeProfiles"})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("ویرایش", "Edit", new { id=item.Id }) |
            @Html.ActionLink("جزئیات", "Details", new { id=item.Id }) |
            @Html.ActionLink("حذف", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
