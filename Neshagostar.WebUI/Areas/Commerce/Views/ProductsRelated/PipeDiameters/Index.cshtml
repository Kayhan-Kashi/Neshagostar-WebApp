@model IEnumerable<Neshagostar.DAL.DataModel.CommerceRelated.ProductsRelated.PipeDiameter>

@{
    ViewBag.Title = "Index";
}

<h2>لیست قطر محصولات</h2>

<p>
    @Html.ActionLink("ثبت", "Create", new { controller="PipeDiameters", area="commerce" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Size)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Size)
        </td>
        <td>
            @Html.ActionLink("ویرایش", "Edit", new { id=item.Id }) |
            @Html.ActionLink("جزئیات", "Details", new { id=item.Id }) |
            @Html.ActionLink("حذف", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
